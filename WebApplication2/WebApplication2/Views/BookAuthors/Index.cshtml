@model IEnumerable<WebApplication2.Models.BookAuthor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AidNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AidNavigation.Aid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Baid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Baid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Baid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
